[error] specs/widget/builds.spec.yaml:5: stdout is a quoted scalar — a stream is written as a block scalar (`|` keeps the final newline, `|-` drops it) (fixable: `node dist/checker.js <root> --fix`) (d4b-spec-block-scalar — see docs/13-linting.md)

conventions checker: 1 error(s) found under {{workdir}}
